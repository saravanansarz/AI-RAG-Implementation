# Azure OpenAI configuration - fill these values or set corresponding environment variables
azure.oai.endpoint=${AZURE_OAI_ENDPOINT:}
azure.oai.key=${AZURE_OAI_KEY:}
azure.oai.deployment=${AZURE_OAI_DEPLOYMENT:}
azure.oai.embeddings-deployment=${AZURE_OAI_EMBEDDINGS_DEPLOYMENT:${AZURE_OAI_DEPLOYMENT:}}

# Azure Cognitive Search (used in dataSources payload)
azure.search.endpoint=${AZURE_SEARCH_ENDPOINT:}
azure.search.key=${AZURE_SEARCH_KEY:}
azure.search.index=${AZURE_SEARCH_INDEX:}

# Path to the PDF in the project root
app.pdf.path=${PDF_PATH:sustainable-finance-impact-report.pdf}

server.port=8080
